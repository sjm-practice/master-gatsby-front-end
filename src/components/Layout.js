import React from 'react';

import Nav from './Nav';
import Footer from './Footer';

const Layout = () => (
  <div>
    <Nav />
    <p>I am a page.</p>
    <Footer />
  </div>
);

export default Layout;
